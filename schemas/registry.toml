# DT Schema Registry v1
# All schemas are versioned, content-addressed, and generate code.

[registry]
version = "1.1"
format = "json-schema-2020-12"
last_updated = "2025-05-09T00:00:00Z"

[[schemas]]
name = "event"
version = "1.0.0"
path = "event/event.schema.json"
description = "Append-only event envelope — single source of truth"
codegen = ["rust", "python", "ts"]

[[schemas]]
name = "knowledge_node"
version = "1.0.0"
path = "knowledge/knowledge_node.schema.json"
description = "Knowledge graph node with rich metadata"
codegen = ["rust", "python", "ts"]

[[schemas]]
name = "metadata_envelope"
version = "1.0.0"
path = "common/metadata_envelope.schema.json"
description = "Metadata envelope attached to every object"
codegen = ["rust", "python", "ts"]

[[schemas]]
name = "vector_clock"
version = "1.0.0"
path = "common/vector_clock.schema.json"
description = "Hybrid vector clock for sync and ordering"
codegen = ["rust", "python", "ts"]

[[schemas]]
name = "sync_peer"
version = "1.0.0"
path = "common/sync_peer.schema.json"
description = "Peer descriptor for mesh sync"
codegen = ["rust", "python", "ts"]
